[package]
name = "revenant"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.25", default-features = false, features = ["png"] }
minifb = "0.28"

[profile.release]
opt-level = 3
lto = true
